# ============================================================================
# CONFIGURACIÓN GENERAL DE LA APLICACIÓN
# ============================================================================
# Estas propiedades se aplican a TODOS los perfiles (dev, test, prod)
# a menos que se sobrescriban con un prefijo específico (%dev, %test, %prod)

# Nombre de la aplicación (aparece en logs y métricas)
quarkus.application.name=evaluacion-crediticia

# ============================================================================
# DATASOURCE - Configuración Base de Datos
# ============================================================================
# Tipo de base de datos: PostgreSQL
# Otras opciones: mysql, mariadb, h2, oracle, mssql
quarkus.datasource.db-kind=postgresql

# Versión específica de PostgreSQL que usaremos (16.10)
# Esto permite a Quarkus usar características específicas de esta versión
quarkus.datasource.db-version=16

# ============================================================================
# HIBERNATE ORM - Gestión del Esquema de Base de Datos
# ============================================================================
# drop-and-create: Borra y recrea las tablas en CADA inicio
# Útil para desarrollo, NUNCA usar en producción
# Otras opciones: create, update, validate, none
quarkus.hibernate-orm.database.generation=drop-and-create

# Mostrar en consola todos los SQL que ejecuta Hibernate
# Útil para debugging y aprendizaje
quarkus.hibernate-orm.log.sql=true

# Archivo SQL que se ejecuta DESPUÉS de crear las tablas
# Ubicación: src/main/resources/import.sql
# Sirve para cargar datos iniciales de prueba
quarkus.hibernate-orm.sql-load-script=import.sql

# ============================================================================
# HTTP - Puertos de la Aplicación
# ============================================================================
# Puerto para modo desarrollo (./mvnw quarkus:dev)
quarkus.http.port=8080

# Puerto para tests (./mvnw test)
# Diferente a 8080 para evitar conflictos si dev está corriendo
quarkus.http.test-port=8081

# ============================================================================
# LOGGING - Configuración de Logs
# ============================================================================
# Nivel de log global: INFO
# Niveles disponibles: TRACE < DEBUG < INFO < WARN < ERROR < FATAL
quarkus.log.level=INFO

# Nivel de log específico para nuestro paquete: DEBUG
# Esto muestra logs más detallados solo de nuestro código
# pe.banco.evaluacion es el package base de nuestro proyecto
quarkus.log.category."pe.banco.evaluacion".level=DEBUG

# ============================================================================
# PERFIL DEV - Desarrollo Local
# ============================================================================
# Este perfil se activa cuando ejecutas: ./mvnw quarkus:dev
# Usa tu PostgreSQL instalado localmente en tu máquina

# Desactivar Dev Services porque usamos PostgreSQL local
# Dev Services levantaría un contenedor Docker, pero no lo necesitamos aquí
%dev.quarkus.devservices.enabled=false

# Driver JDBC de PostgreSQL (ya viene en las dependencias)
%dev.quarkus.datasource.jdbc.driver=org.postgresql.Driver

# URL de conexión a PostgreSQL local
# Formato: jdbc:postgresql://host:puerto/nombre_base_datos
# localhost:5432 = PostgreSQL local en puerto por defecto
# postgres = nombre de la base de datos
%dev.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/postgres

# Usuario de PostgreSQL (ajustar según tu instalación)
%dev.quarkus.datasource.username=rodrigosilva

# Password vacía (sin contraseña en desarrollo local)
# Si tu PostgreSQL tiene password, colocarla aquí
%dev.quarkus.datasource.password=

# Versión de PostgreSQL (16.x)
%dev.quarkus.datasource.db-version=16

# Recrear tablas en cada inicio (útil en desarrollo)
%dev.quarkus.hibernate-orm.database.generation=drop-and-create

# Mostrar SQL en consola (útil para aprender y debugging)
%dev.quarkus.hibernate-orm.log.sql=true

# ============================================================================
# PERFIL PROD para Tests de Integración Nativos
# ============================================================================
# Cuando corres mvn verify -Dnative, el binario nativo se ejecuta con 
# perfil prod, por eso necesita estas configuraciones
%prod.quarkus.datasource.jdbc.driver=org.postgresql.Driver
%prod.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/postgres
%prod.quarkus.datasource.username=rodrigosilva
%prod.quarkus.datasource.password=
%prod.quarkus.datasource.db-version=16

# ============================================================================
# PERFIL TEST - Tests Automatizados
# ============================================================================
# Este perfil se activa automáticamente cuando ejecutas: ./mvnw test
#
# ⚡ IMPORTANTE - Dev Services Automático:
# Como NO hay configuración de URL, username ni password para %test,
# Quarkus activa automáticamente "Dev Services" que:
#
# 1. Detecta que necesitas PostgreSQL
# 2. Levanta automáticamente un contenedor Docker con postgres:16-alpine
# 3. Crea una base de datos temporal
# 4. Ejecuta los tests contra esa base de datos
# 5. Destruye el contenedor al terminar los tests
#
# ✅ VENTAJAS:
# - No necesitas tener PostgreSQL instalado para correr tests
# - Cada ejecución de tests usa una BD limpia (aislamiento)
# - No contamina tu base de datos de desarrollo
# - Los tests son reproducibles en cualquier máquina con Docker
#
# 📋 REQUISITOS:
# - Docker Desktop debe estar corriendo
# - No configurar jdbc.url, username ni password en %test
#
# 🎯 RESULTADO:
# ./mvnw test → Levanta Docker automático con PostgreSQL 16
# Los tests corren contra esa BD temporal
# Al terminar, el contenedor se destruye
#
# NO agregar estas líneas (impedirían Dev Services):
# %test.quarkus.datasource.jdbc.url=...
# %test.quarkus.datasource.username=...
# %test.quarkus.datasource.password=...

# Si quieres especificar la imagen de PostgreSQL para tests (opcional):
# %test.quarkus.datasource.devservices.image-name=postgres:16-alpine

# ============================================================================
# RESUMEN DE PERFILES
# ============================================================================
# ./mvnw quarkus:dev              → Perfil DEV  → PostgreSQL 16 local (localhost:5432)
# ./mvnw test                     → Perfil TEST → Docker automático (Dev Services con PostgreSQL 16)
# java -jar -Dquarkus.profile=prod → Perfil PROD → Variables de entorno

# ============================================================================
# PERFIL TEST - Usar PostgreSQL Local en lugar de Dev Services
# ============================================================================
%test.quarkus.devservices.enabled=false
%test.quarkus.datasource.jdbc.driver=org.postgresql.Driver
%test.quarkus.datasource.jdbc.url=jdbc:postgresql://localhost:5432/postgres
%test.quarkus.datasource.username=rodrigosilva
%test.quarkus.datasource.password=
%test.quarkus.datasource.db-version=16